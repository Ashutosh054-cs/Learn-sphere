# Copy this file to `.env.local` and fill the values. Do NOT commit `.env.local`.
# Example .env.local for Learn Sphere (Supabase)

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# Notes:
# - Create `.env.local` in the project root (next to package.json).
# - Restart the dev server after changing environment variables.
# - `.env.local` is ignored by git by default (pattern `*.local` in .gitignore).
# - Keep your anon key secret; do not commit it to source control.
